import {describe,it,expect} from "vitest"; import {MockLLM} from "../providers/mock"; describe("pipeline primitives",()=>{it("creates script without external API",async()=>{const x=await new MockLLM().generateScript({title:"Espaço",language:"pt-BR",style:"documentário",duration:5});expect(x).toContain("Espaço")});it("breaks scenes",async()=>{const x=await new MockLLM().breakScenes("A\n\nB","");expect(x).toHaveLength(2)})})