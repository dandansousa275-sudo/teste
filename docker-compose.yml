services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      DATABASE_URL: file:./data/app.db
      STORAGE_PATH: ./storage
    volumes:
      - ./data:/app/data
      - ./storage:/app/storage
